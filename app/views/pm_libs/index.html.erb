<h1>Listing pm_libs</h1>

<table>
  <tr>
	 <td>名称</td>             
    <td></td>  
  </tr>

<% @pm_libs.each do |pm_lib| %>
  <tr>
    <td><%= link_to pm_lib.name, pm_lib %></td>             
    <td><%= link_to 'Destroy', pm_lib, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New pm_lib', new_pm_lib_path %>