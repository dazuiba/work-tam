<h1>新建<%= @project.project_type_text %></h1>
<% labelled_tabular_form_for :project, @project, :url => { :action => "new", :id => @project }, :method=>'post' do |f| %>
  <%= render :partial => 'form', :locals => {:f => f}  %>
  <%= submit_tag "保存", :class => 'default' %>
<% end -%>
